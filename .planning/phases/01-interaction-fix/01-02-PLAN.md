---
phase: 01-interaction-fix
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/TagLibrary/TagItem.tsx
  - src/components/TagLibrary/CategoryGroup.tsx
  - src/components/Workspace/WorkspaceTag.tsx
autonomous: false
requirements:
  - INTR-01
  - INTR-02
  - INTR-03

must_haves:
  truths:
    - "Clicking a tag in the negative category adds it to negativeTags, not selectedTags"
    - "Clicking an already-active negative-category tag removes it from negativeTags"
    - "Negative-category tags in the tag library have red-tinted styling distinct from positive-category tags"
    - "Negative-category tags in the tag library show deepened red when active (in negativeTags), distinct from violet selected state"
    - "Category title for negative category displays in red text"
    - "WorkspaceTag shows a visible arrow button on hover to move between positive/negative areas"
    - "WorkspaceTag no longer uses onContextMenu for move operations"
    - "Positive workspace tags show down-arrow (move to negative), negative workspace tags show up-arrow (move to positive)"
  artifacts:
    - path: "src/components/TagLibrary/TagItem.tsx"
      provides: "Category-aware click behavior + negative styling"
      contains: "isNegativeCategory"
    - path: "src/components/TagLibrary/CategoryGroup.tsx"
      provides: "Red title for negative category"
      contains: "negative"
    - path: "src/components/Workspace/WorkspaceTag.tsx"
      provides: "Visible move buttons replacing context menu"
      min_lines: 40
  key_links:
    - from: "src/components/TagLibrary/TagItem.tsx"
      to: "src/stores/usePromptStore.ts"
      via: "addNegativeTag/removeNegativeTag selectors"
      pattern: "addNegativeTag|removeNegativeTag"
    - from: "src/components/Workspace/WorkspaceTag.tsx"
      to: "src/stores/usePromptStore.ts"
      via: "moveToNegative/moveToPositive selectors"
      pattern: "moveToNegative|moveToPositive"
    - from: "src/components/TagLibrary/TagItem.tsx"
      to: "tag.category"
      via: "category check for click behavior branching"
      pattern: "tag\\.category.*negative"
---

<objective>
Implement all UI-facing interaction fixes: negative-category tag click behavior, visual distinction for negative tags in the library, and visible move buttons on workspace tags.

Purpose: After Plan 01 hardened the store, this plan wires the corrected store behavior into the UI components so users experience the right interactions.
Output: TagItem with category-aware click + red styling, CategoryGroup with red title, WorkspaceTag with visible arrow buttons.
</objective>

<execution_context>
@C:/Users/hmoel/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/hmoel/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-interaction-fix/01-CONTEXT.md
@.planning/phases/01-interaction-fix/01-RESEARCH.md
@.planning/phases/01-interaction-fix/01-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types/index.ts:
```typescript
export interface Tag {
  id: string
  text: string
  category: string
  aliases?: string[]
  description?: string
  platforms?: Platform[]
}

export interface Category {
  id: string
  name: string
  icon?: string
  order: number
}
```

From src/data/categories.ts:
```typescript
export const builtinCategories: Category[] = [
  { id: 'quality', name: '质量', order: 0 },
  // ... 8 more categories ...
  { id: 'negative', name: '负面常用', order: 9 },
]
```

From src/stores/usePromptStore.ts (relevant store actions):
```typescript
// After Plan 01, these are atomic single-set() operations:
toggleTag: (tagId: string) => void
addNegativeTag: (tagId: string) => void
removeNegativeTag: (tagId: string) => void
moveToNegative: (tagId: string) => void  // atomic
moveToPositive: (tagId: string) => void  // atomic
```

From src/components/TagLibrary/TagItem.tsx (current):
```typescript
interface TagItemProps {
  tag: Tag
}
// Currently calls toggleTag on all clicks regardless of category
// Has isSelected (checks selectedTags) and isNegative (checks negativeTags) selectors
```

From src/components/TagLibrary/CategoryGroup.tsx (current):
```typescript
interface CategoryGroupProps {
  category: Category
  tags: Tag[]
}
// Title has no category-specific styling
```

From src/components/Workspace/WorkspaceTag.tsx (current):
```typescript
interface WorkspaceTagProps {
  tagId: string
  weight?: number
  isNegative?: boolean
}
// Uses onContextMenu for move operations
// Has group-hover pattern for weight buttons (positive only)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TagItem category-aware click + negative styling, CategoryGroup red title</name>
  <files>src/components/TagLibrary/TagItem.tsx, src/components/TagLibrary/CategoryGroup.tsx</files>
  <action>
    **TagItem.tsx -- Category-aware click behavior (INTR-01):**
    1. Add store selectors for `addNegativeTag` and `removeNegativeTag`
    2. Derive `isNegativeCategory` from `tag.category === 'negative'`
    3. Replace the click handler: if `isNegativeCategory`, toggle based on `isNegative` state (call `removeNegativeTag` if already in negativeTags, else `addNegativeTag`). If positive category, call existing `toggleTag`.
    4. The `toggleTag` selector is still needed for positive-category tags.

    **TagItem.tsx -- Negative visual distinction (INTR-03):**
    Update the className conditional to handle 4 states:
    1. Positive-category, selected (isSelected): `bg-violet-600 text-white` (existing)
    2. Positive-category, default: `bg-zinc-800 text-zinc-300 hover:bg-zinc-700` (existing)
    3. Negative-category, active (isNegative): `bg-red-800/60 text-red-200` (deepened red, distinct from violet)
    4. Negative-category, default: `bg-red-900/30 text-red-400 hover:bg-red-900/50` (muted red tint)

    The styling ternary chain becomes:
    ```
    isNegativeCategory
      ? (isNegative ? 'bg-red-800/60 text-red-200' : 'bg-red-900/30 text-red-400 hover:bg-red-900/50')
      : (isSelected ? 'bg-violet-600 text-white' : 'bg-zinc-800 text-zinc-300 hover:bg-zinc-700')
    ```

    **CategoryGroup.tsx -- Red title for negative category (INTR-03):**
    1. Derive `isNegativeCategory` from `category.id === 'negative'`
    2. Apply conditional red text to the category name span: `text-red-400` when negative, leave default otherwise.
    3. Also apply red tint to the count badge: `text-red-500/70` when negative, existing `text-zinc-600` otherwise.

    **Do NOT:**
    - Change TagItem props (tag.category is already available on the Tag object)
    - Add a constant for the negative category ID (acceptable for 2 usages across 2 files -- KISS per project principles)
    - Touch WorkspaceTag in this task (that is Task 2)
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build</automated>
  </verify>
  <done>TagItem clicks route negative-category tags to negativeTags (not selectedTags). Negative-category tags show red tint in resting state and deepened red when active. CategoryGroup shows red title for negative category. TypeScript compiles clean, build succeeds.</done>
</task>

<task type="auto">
  <name>Task 2: WorkspaceTag visible move buttons replacing context menu</name>
  <files>src/components/Workspace/WorkspaceTag.tsx</files>
  <action>
    **Replace onContextMenu with visible arrow buttons (INTR-02):**

    1. Remove the `handleContextMenu` function entirely.
    2. Remove `onContextMenu={handleContextMenu}` from the outer div.
    3. Remove the `title` attribute that says "Right-click: move to..." from the outer div.
    4. Add a hover-visible arrow button using the existing `group` + `hidden group-hover:flex` pattern (already used for weight buttons).

    **Button placement:** Place the move button between the tag text/weight area and the X delete button. It should appear on hover alongside the existing weight buttons (for positive tags) or standalone (for negative tags).

    **Button behavior:**
    - Positive tags (isNegative === false): Show a down-arrow button (move to negative). On click, call `moveToNegative(tagId)`.
    - Negative tags (isNegative === true): Show an up-arrow button (move to positive). On click, call `moveToPositive(tagId)`.

    **Arrow SVG (inline, following project pattern):**
    - Down arrow (for positive tags): `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" className="w-3 h-3"><path fillRule="evenodd" d="M8 2a.75.75 0 0 1 .75.75v8.69l3.22-3.22a.75.75 0 1 1 1.06 1.06l-4.5 4.5a.75.75 0 0 1-1.06 0l-4.5-4.5a.75.75 0 0 1 1.06-1.06l3.22 3.22V2.75A.75.75 0 0 1 8 2Z" clipRule="evenodd" /></svg>`
    - Up arrow (for negative tags): `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" className="w-3 h-3"><path fillRule="evenodd" d="M8 14a.75.75 0 0 1-.75-.75V4.56L4.03 7.78a.75.75 0 0 1-1.06-1.06l4.5-4.5a.75.75 0 0 1 1.06 0l4.5 4.5a.75.75 0 0 1-1.06 1.06L8.75 4.56v8.69A.75.75 0 0 1 8 14Z" clipRule="evenodd" /></svg>`

    **Implementation structure for the hover buttons area:**
    Create a single `hidden group-hover:flex` span that contains:
    - For positive tags: weight buttons (existing -/+) AND the down-arrow move button
    - For negative tags: only the up-arrow move button (no weight buttons, matching current behavior)

    The move button styling: `w-4 h-4 flex items-center justify-center text-zinc-500 hover:text-zinc-300 rounded hover:bg-zinc-700 cursor-pointer` (consistent with existing weight button style).

    Add `title` to the move button: "移至负面" for positive tags, "移至正面" for negative tags.

    **Do NOT:**
    - Keep onContextMenu as a secondary interaction -- the locked decision says to remove it entirely
    - Add weight buttons to negative tags (current behavior is no weight on negative, keep it)
    - Change the WorkspaceTagProps interface
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build</automated>
  </verify>
  <done>WorkspaceTag has no onContextMenu handler. Hover reveals arrow button (down for positive tags, up for negative tags). Clicking the arrow moves the tag to the opposite area. Build succeeds, no TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify all Phase 1 interaction fixes in browser</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 1 interaction fixes in the browser.

    **What was built:**
    1. Negative-category tags in tag library have red tint (muted at rest, deepened when active)
    2. Clicking negative-category tags adds to Negative area (not Positive)
    3. Clicking an active negative-category tag removes it from Negative area
    4. Negative category title shows in red
    5. WorkspaceTag hover reveals arrow button to move between positive/negative
    6. Right-click context menu removed from workspace tags

    **How to verify:**
    1. Run `npx vite dev` and open http://127.0.0.1:5173/nekoPrompt/ in browser
    2. In the left sidebar, expand the "负面常用" category:
       - Verify the category title text is red
       - Verify tags inside have a red-tinted background (not gray like other categories)
    3. Click a negative-category tag (e.g., "lowres"):
       - Verify it appears in the Negative section of the workspace (NOT in Positive)
       - Verify the tag in the sidebar shows deepened red (not violet)
    4. Click the same tag again in the sidebar:
       - Verify it is removed from the Negative section
    5. Click a positive-category tag (e.g., "masterpiece"):
       - Verify it still appears in the Positive section as before (violet in sidebar)
    6. Hover over a tag in the Positive workspace area:
       - Verify an arrow-down button appears (alongside weight +/- buttons)
       - Click the arrow -- verify tag moves to Negative area
    7. Hover over a tag in the Negative workspace area:
       - Verify an arrow-up button appears
       - Click the arrow -- verify tag moves to Positive area
    8. Right-click a workspace tag:
       - Verify NO context menu for moving appears (browser default context menu is fine)
  </action>
  <verify>Human visual inspection in browser</verify>
  <done>All 8 verification steps pass. User types "approved".</done>
</task>

</tasks>

<verification>
1. `npx vitest run` -- all tests pass (25 tests)
2. `npx tsc --noEmit` -- no TypeScript errors
3. `npx vite build` -- build succeeds
4. Manual verification of all 4 INTR requirements in browser
</verification>

<success_criteria>
- Negative-category tag clicks route to negativeTags (INTR-01)
- Workspace tags have visible move buttons on hover, no context menu (INTR-02)
- Negative-category tags visually distinct with red tones in tag library (INTR-03)
- Category title "负面常用" displays in red (INTR-03)
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-interaction-fix/01-02-SUMMARY.md`
</output>
